%h3
  Welcome
%p
  = link_to 'Users:', users_path
  = User.count
  registered
%p
  %ul
    - WikiSpace.all.each do |space|
      %li
        = link_to space.title + " Wiki", wiki_space_wiki_page_path(space, "start")
